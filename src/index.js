import logger from './logger'
import plutusService from './services/plutus.service';
import fmpService from './services/fmp.service';

async function init() {
  //const stocks = await plutusService.getStocks();
  //const stocks = await fmpService.getStockList();
  //logger.debug(stocks);
  // const stockData = await fmpService.getStockData('AAPL');
  // logger.debug(stockData);
}

init();